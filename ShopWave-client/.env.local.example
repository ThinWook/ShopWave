# Frontend env example
# Prefer leaving this empty to use same-origin + Next rewrites (/api/v1/*).
# Only set when frontend must call backend directly (cross-origin deployment).
NEXT_PUBLIC_API_BASE_URL=

# Google OAuth client ID from Google Cloud Console
NEXT_PUBLIC_GOOGLE_CLIENT_ID=Y335462284581-a0omnmanjlsvs0b8rumkuf384tgdot7v.apps.googleusercontent.com
