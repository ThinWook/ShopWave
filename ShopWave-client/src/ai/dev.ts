import { config } from 'dotenv';
config();

// AI recommendation flow (Gemini) has been disabled/removed. This file
// previously imported the product recommendation flow for local/dev usage.
// Kept dotenv init for any dev scripts that rely on environment variables.