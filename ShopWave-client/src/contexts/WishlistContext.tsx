
// REMOVED: wishlist context deleted per project request.
// The original wishlist feature has been fully removed. This file is intentionally
// left as an empty marker (no exports) to avoid accidental re-use. If you need
// the file physically deleted from the repo, run a manual git removal.

export {};
